# AllFi 环境变量配置
# 复制为 .env 后修改

# 加密主密钥（必须修改！使用 openssl rand -base64 32 生成）
ALLFI_MASTER_KEY=CHANGE_ME_USE_openssl_rand_base64_32

# Etherscan API Key（免费申请 https://etherscan.io/apis）
ETHERSCAN_API_KEY=

# BscScan API Key（免费申请 https://bscscan.com/apis）
BSCSCAN_API_KEY=

# CoinGecko API Key（可选）
COINGECKO_API_KEY=

# 服务端口（前端 + API 统一端口）
ALLFI_PORT=5173

# 时区配置（默认：Asia/Shanghai，可选：UTC、America/New_York 等）
# 容器会自动挂载宿主机时区文件，此变量用于显式指定时区
TZ=Asia/Shanghai

# === 代理配置（国内用户可按需启用，取消注释并修改） ===
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1,backend
# 详细说明见 docs/guides/proxy-guide.md
